import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    headerStyle: {
        height: 70, //Modica el tamaño de la altura del header
        elevation: 0,
        shadowOpacity: 0,
        borderBottomWidth: 0,
    },
});